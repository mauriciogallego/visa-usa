# Testing webs with Cypress 

This code is made for lerning purpose.


# Getting Started

# Install
First, install necessary package to be able to run the code:

```bash
yarn add
```
# Run

If you want to see how the interface runs:

```bash
yarn cypress
```

otherwise:

```bash
yarn cypress:headless
```
